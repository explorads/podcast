plugins {
    id("com.android.library")
}
apply from: "../../common.gradle"

android {
    namespace "com.explorads.podcast.ui.appstartintent"
}

dependencies {
    annotationProcessor "androidx.annotation:annotation:$annotationVersion"
}
